'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.serializeImport = serializeImport;
exports.serializeChunk = serializeChunk;
function serializeImport(fileImport) {
  return (fileImport.from || 'null') + '::' + fileImport.request + '::' + (fileImport.resolved || 'null');
}

function serializeChunk(fileChunk) {
  var imports = fileChunk.imports.map(serializeImport);
  var entries = fileChunk.entries.map(serializeImport);
  return JSON.stringify({ imports: imports, entries: entries });
}