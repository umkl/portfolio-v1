'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _sbExec = require('sb-exec');

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; }

exports.default = {
  install: function install(name, save, rootDirectory) {
    return _asyncToGenerator(function* () {
      var parameters = ['install'];
      if (save) {
        parameters.push('--save');
      }
      parameters.push(name, '--loglevel=error', '--no-color');
      var result = yield (0, _sbExec.exec)('npm', parameters, {
        cwd: rootDirectory,
        stream: 'stderr',
        allowEmptyStderr: true
      });
      if (result && result.indexOf('npm ERR') !== -1) {
        throw new Error('NPM Error: ' + result);
      }
    })();
  }
};